{
  "name": "cargaison",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "mkdir -p public/dist && cp -r src/views public/ && cp -r dist/* public/dist/",
    "start": "php -S 0.0.0.0:${PORT:-3000} -t public/",
    "dev": "concurrently \"npm run build -- --watch\" \"npm start\"",
    "deploy": "npm run build && npm start"
  },
  "engines": {
    "node": ">=18.0.0",
    "php": "^8.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "concurrently": "^8.0.0"
  },
  "keywords": [
    "typescript",
    "cargaison",
    "logistics"
  ],
  "author": "",
  "license": "ISC",
  "description": "Système de gestion de cargaisons"
}
