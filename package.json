{
  "name": "cargaison",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "postbuild": "mkdir -p public/dist && cp -r src/views public/ && cp -r dist/* public/dist/",
    "start": "php -S 0.0.0.0:$PORT -t public/",
    "json-server": "json-server db.json --port 3000",
    "dev:full": "concurrently \"npm run json-server\" \"npm run start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "typescript",
    "cargaison",
    "logistics"
  ],
  "author": "",
  "license": "ISC",
  "description": "Système de gestion de cargaisons",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
