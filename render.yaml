services:
  - type: web
    name: yonima-cargaison
    env: php
    buildCommand: |
      mkdir -p /data
      cp -r data/* /data/
      npm install
      npm run build
    startCommand: php -S 0.0.0.0:$PORT -t public/
    envVars:
      - key: DATA_PATH
        value: /data
    disk:
      name: data
      mountPath: /data
      sizeGB: 1